// Write a JavaScript program to display and converts minutes into seconds

function convertMinToSec(minutes) {
    return minutes * 60;
}

let minutes = 5;
let seconds = convertMinToSec(minutes);

console.log(minutes + " minutes is equal to " + seconds + " seconds.");



