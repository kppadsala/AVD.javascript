// write a javascript program to Return the Next Number from the Integer Passed. 

function getNextNumber(num) {
    return num + 1;
  }
  
  const inputNumber = -1;
  const nextNumber = getNextNumber(inputNumber);
  console.log(nextNumber); 
  